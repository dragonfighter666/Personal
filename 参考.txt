这份需求文档非常详尽，非常适合转化为结构化的开发指令。为了让 Cursor 能够编写出结构清晰、易于维护的代码，我们需要将需求转化为“技术语言”，明确**技术栈**、**数据库设计**、**API接口定义**以及**模块拆分**。

以下是为您整理的完整 Prompt，您可以直接复制发送给 Cursor。

---

### 发送给 Cursor 的 Prompt 内容

**项目名称**：社区一体化融合服务平台
**项目角色**：你是一名拥有10年经验的全栈架构师。
**当前任务**：请根据以下需求分析、数据库设计大纲和接口定义，为我搭建项目基础框架，并编写核心模块的代码。

#### 1. 技术栈选型与架构要求
为了保证代码的可维护性，请遵循以下架构规范：
*   **后端**：Java (Spring Boot 框架) + MyBatis-Plus。
*   **数据库**：MySQL。
*   **前端（居民端/移动端）**：Uni-app (Vue 3 语法)。
*   **前端（管理端/物业/系统后台）**：Vue 3 + Element Plus。
*   **代码规范**：
    *   后端采用标准分层架构：`Controller` -> `Service` (接口 + 实现) -> `Mapper` -> `Entity`。
    *   使用 `DTO` 进行数据传输，`Entity` 与数据库映射。
    *   统一返回结果封装 (`Result` 类)，统一异常处理 (`GlobalExceptionHandler`)。
    *   使用 MyBatis-Plus 逻辑删除字段 `is_deleted` 和自动填充字段 `create_time`, `update_time`。

#### 2. 用户角色定义
系统包含四种角色，请使用 RBAC (基于角色的访问控制) 模型设计权限：
1.  **居民**：使用移动端/小程序。
2.  **物业管理员**：使用 Web 管理后台。
3.  **维修工**：使用移动端接收任务。
4.  **系统管理员**：使用 Web 管理后台，拥有最高权限。

#### 3. 数据库设计指导
请根据以下核心实体生成数据库建表 SQL 语句，字段类型请自行推断最优解：

1.  **sys_user (用户表)**：基础信息、角色类型、关联房屋 ID。
2.  **community_building / community_room (楼栋/房屋表)**：楼栋号、单元、房号、业主 ID。
3.  **repair_order (报修工单表)**：
    *   关键字段：申请人 ID、报修内容、图片、状态（待指派/处理中/已退回/已完成）、维修工 ID、驳回理由、评价。
4.  **complaint (投诉建议表)**：内容、处理人、处理状态、反馈内容。
5.  **fee_bill (费用账单表)**：用户 ID、费用类型（水电/物业）、金额、缴费截止日期、状态（未缴/待审核/已缴纳/已驳回）。
6.  **payment_record (缴费记录表)**：关联账单 ID、支付方式（线上/线下）、凭证图片、审核状态、驳回理由。
7.  **visitor_apply (访客申请表)**：访客姓名、电话、来访时间、审核状态、通行码。
8.  **access_log (门禁日志表)**：进出人员 ID/访客 ID、进出时间、类型（居民/访客）、抓拍图片。
9.  **parking_spot (车位表)**：车位号、状态（空闲/占用）。
10. **parking_reservation (车位预约表)**：用户 ID、车位 ID、预约时间段、状态。
11. **notice (公告表)**：标题、内容、发布时间、类型。
12. **activity (社区活动表)**：活动内容、时间、报名人数限制。
13. **activity_signup (活动报名表)**：关联活动 ID、用户 ID。
14. **neighbor_post (邻里互助表)**：发布人 ID、内容、类型（求助/帮助）、互动评论。
15. **vote_topic (投票议题表)**：标题、选项、截止时间、当前票数。
16. **vote_record (投票记录表)**：议题 ID、用户 ID、选项。
17. **merchant (商户表)**：名称、地点、营业状态。
18. **device_facility (设备设施表)**：设备名称、位置、维护计划、状态。
19. **sys_log (系统日志表)**：操作人、操作内容、IP、时间。

#### 4. 核心功能模块拆解与接口设计
请按照模块化方式编写代码，以下是各模块的详细接口需求：

**模块一：系统服务模块**
*   `POST /auth/login`：通用登录接口，根据账号角色返回不同 Token 和路由信息。
*   `GET /user/info`：获取当前用户信息。
*   `PUT /user/info`：更新个人信息（居民维护房产、联系方式）。

**模块二：物业服务模块 (Service - Repair & Complaint)**
*   **报修流程**：
    *   `POST /resident/repair`：居民提交报修。
    *   `GET /admin/repair/list`：物业查看报修列表（支持状态筛选）。
    *   `POST /admin/repair/assign`：物业指派维修工。
    *   `GET /worker/repair/list`：维修工查看自己的任务。
    *   `POST /worker/repair/complete`：维修工提交完成（上传修后图片）。
    *   `POST /resident/repair/confirm`：居民确认验收或退回（填写驳回理由）。
*   **投诉建议**：
    *   `POST /resident/complaint`：居民提交投诉。
    *   `GET /admin/complaint/list`：物业列表。
    *   `POST /admin/complaint/reply`：物业回复处理。

**模块三：财务缴费模块**
*   `POST /admin/fee/bill`：物业生成账单并推送给居民。
*   `GET /resident/fee/list`：居民查看待缴费账单。
*   `POST /resident/fee/pay`：居民提交缴费（上传线下凭证图片 或 线上支付模拟，状态变更为“待审核”）。
*   `POST /admin/fee/audit`：物业审核缴费凭证（通过/驳回，填写驳回理由）。

**模块四：安防与通行模块**
*   **访客**：
    *   `POST /resident/visitor/apply`：居民申请访客。
    *   `POST /admin/visitor/audit`：物业审核。
    *   `GET /resident/visitor/qrcode`：生成访客通行码（生成唯一 Code）。
*   **门禁**：
    *   `GET /resident/access/qrcode`：生成居民临时门禁码。
    *   `POST /admin/access/log`：门禁记录录入（模拟开门记录）。
    *   `GET /admin/access/monitor`：监控列表与异常提醒（返回模拟视频文件路径）。

**模块五：社区生活模块**
*   `GET /resident/notice/list`：公告列表。
*   `GET /resident/activity/list`：活动列表。
*   `POST /resident/activity/signup`：报名活动。
*   `GET /resident/parking/available`：查询空闲车位。
*   `POST /resident/parking/reserve`：预约车位。
*   `POST /resident/post/publish`：发布邻里互助。
*   `POST /resident/vote/submit`：提交投票。

**模块六：后台管理模块**
*   `GET /admin/dashboard/stats`：数据统计看板（报修数、投诉数、缴费率等）。
*   `POST /admin/merchant`：添加/修改商户。
*   `POST /admin/device`：添加设备维护计划。
*   `POST /system/user`：系统管理员管理账号。
*   `GET /system/log`：查看操作日志。

#### 5. 编码任务指令
请按照以下步骤开始编写代码：

1.  **第一步**：生成项目目录结构（Spring Boot 标准），配置 `application.yml` (数据库连接配置留空，由我填写)。
2.  **第二步**：编写所有实体类 (`Entity`)，并包含 MyBatis-Plus 的注解 (`@TableName`, `@TableId` 等)。
3.  **第三步**：编写报修工单模块 (`RepairOrder`) 的完整代码，包括 Controller, Service, Mapper, DTO。这是业务逻辑最复杂的模块，请确保状态流转逻辑正确。
4.  **第四步**：编写前端（Uni-app）的报修提交页面和进度跟踪页面。

---

### 针对您需求的拆解思路（供您参考）

为了让您明白我为什么这样拆解，以下是针对您描述的模糊想法的具体转化逻辑：

#### 1. 居民端
*   **模糊想法**：“个人信息管理：维护个人信息、房产信息等”
    *   **功能点**：用户基本资料修改、房产绑定申请（需后台审核或自动认证）、紧急联系人设置。
*   **模糊想法**：“物业报修：跟踪维修进度”
    *   **功能点**：提交工单（图文）、工单列表查询、工单详情（时间轴展示进度）、评价/验收功能。
*   **模糊想法**：“费用缴纳...支持线上扫码...线下缴费...审核驳回”
    *   **功能点**：这是一个复杂的流程。
        *   状态机：未缴费 -> 待审核(用户提交凭证) -> 已完成(审核通过) / 已驳回(审核不通过) -> 重新提交。
        *   界面：账单列表、缴费详情、凭证上传组件、驳回原因提示框。
*   **模糊想法**：“智能客服...转人工”
    *   **功能点**：简单的关键词自动回复（前端写死或后端配置） + “转人工”按钮（跳转至在线留言或投诉建议板块，或者模拟客服聊天窗口）。

#### 2. 物业管理员端
*   **模糊想法**：“报修工单处理...指派...全流程跟踪”
    *   **功能点**：待指派工单列表、指派操作（选择维修工下拉框）、工单进度监控大屏、历史工单查询。
*   **模糊想法**：“门禁管理...插入视频...异常提醒”
    *   **功能点**：
        *   门禁权限配置（增删改查）。
        *   监控中心页面：左侧为摄像头列表，右侧为视频播放区（前端放一个静态视频文件地址），下方为异常日志列表。
*   **模糊想法**：“数据统计看板”
    *   **功能点**：使用 ECharts 展示。包括：本月报修类别饼图、投诉趋势折线图、缴费率柱状图。

#### 3. 维修工端
*   **模糊想法**：“维修分配查看...查看退回理由”
    *   **功能点**：
        *   待办任务列表（状态：待处理）。
        *   已办任务列表（状态：已完成/已退回）。
        *   任务详情页：包含报修图片、居民描述、维修结果提交（拍照上传）、退回原因查看（只读）。

#### 4. 系统管理员端
*   **模糊想法**：“日志管理、数据备份、角色权限”
    *   **功能点**：
        *   用户管理：重置密码、修改用户状态。
        *   角色管理：配置菜单权限（树形结构）。
        *   日志管理：AOP 切面记录所有接口操作，存入 `sys_log` 表，提供分页查询界面。
        *   数据备份：通常调用数据库脚本执行，前端提供一个“立即备份”按钮和备份记录列表。

### 建议
在让 Cursor 生成代码时，建议**分步进行**。
1.  先让它生成**数据库设计**和**实体类**。
2.  确认无误后，让它生成**基础框架**和**公共模块**（如 Result 封装、异常处理）。
3.  最后按**模块**逐个生成业务代码（例如：“请先帮我写报修模块的代码”），这样代码质量最高，也便于您检查和维护。